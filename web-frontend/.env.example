# Base URL of the Jellyfin server, used by the Vite dev proxy for /api requests.
# Use the hostname (not an IP) so the proxied Host header matches Caddy's site block.
VITE_JELLYFIN_URL=http://orangepi3b.local
